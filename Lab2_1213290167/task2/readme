To edit the database url, username and password change the init parameters in the web.xml file. Web.xml file which is located in the main folder.

note- add phoneentry adds the entry to the database but it throws exception after that and gets redirected to exception page but I was unable to figure out why it was happening.